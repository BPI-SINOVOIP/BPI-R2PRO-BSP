noinst_PROGRAMS = camera-demo
camera_demo_CFLAGS =                            \
            $(GST_CFLAGS)                       \
	    $(GST_BASE_CFLAGS)                  \
            $(GST_PLUGINS_BASE_CFLAGS)          \
            $(GST_VIDEO_CFLAGS)                 \
            $(NULL)

camera_demo_LDADD =                             \
            $(GST_LIBS)                         \
            $(GST_BASE_LIBS)                    \
            $(GST_PLUGINS_BASE_LIBS)            \
            $(GST_VIDEO_CFLAGS)                 \
            $(NULL)
