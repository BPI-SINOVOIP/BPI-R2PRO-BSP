SRC_URI_pn-linux-rockchip = " \
	git://${TOPDIR}/../../kernel;protocol=file;usehead=1 \
	file://cgroups.cfg \
"
SRCREV_pn-linux-rockchip = "${AUTOREV}"
KBRANCH = "HEAD"

SRC_URI_pn-linux-libc-headers = " \
	git://${TOPDIR}/../../kernel;protocol=file;usehead=1 \
"
SRCREV_pn-linux-libc-headers = "${AUTOREV}"
