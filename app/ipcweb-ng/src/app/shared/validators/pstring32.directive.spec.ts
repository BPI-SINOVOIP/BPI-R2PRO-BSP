import { Pstring32Directive } from './pstring32.directive';

describe('Pstring32Directive', () => {
  it('should create an instance', () => {
    const directive = new Pstring32Directive();
    expect(directive).toBeTruthy();
  });
});
