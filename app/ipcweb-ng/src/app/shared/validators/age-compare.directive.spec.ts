import { AgeCompareDirective } from './age-compare.directive';

describe('AgeCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new AgeCompareDirective();
    expect(directive).toBeTruthy();
  });
});
