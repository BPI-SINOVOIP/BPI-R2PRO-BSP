import { IsDecimalDirective } from './is-decimal.directive';

describe('IsDecimalDirective', () => {
  it('should create an instance', () => {
    const directive = new IsDecimalDirective();
    expect(directive).toBeTruthy();
  });
});
