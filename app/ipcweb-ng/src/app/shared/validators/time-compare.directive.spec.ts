import { TimeCompareDirective } from './time-compare.directive';

describe('TimeCompareDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeCompareDirective();
    expect(directive).toBeTruthy();
  });
});
