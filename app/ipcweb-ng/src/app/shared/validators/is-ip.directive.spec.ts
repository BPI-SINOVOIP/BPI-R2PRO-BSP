import { IsIpDirective } from './is-ip.directive';

describe('IsIpDirective', () => {
  it('should create an instance', () => {
    const directive = new IsIpDirective();
    expect(directive).toBeTruthy();
  });
});
