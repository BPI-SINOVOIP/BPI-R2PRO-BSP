import { IsStandardTimeDirective } from './is-standard-time.directive';

describe('IsStandardTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsStandardTimeDirective();
    expect(directive).toBeTruthy();
  });
});
