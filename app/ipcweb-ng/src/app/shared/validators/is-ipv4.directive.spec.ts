import { IsIpv4Directive } from './is-ipv4.directive';

describe('IsIpv4Directive', () => {
  it('should create an instance', () => {
    const directive = new IsIpv4Directive();
    expect(directive).toBeTruthy();
  });
});
