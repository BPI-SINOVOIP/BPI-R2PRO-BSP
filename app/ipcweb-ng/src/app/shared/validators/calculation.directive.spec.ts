import { CalculationDirective } from './calculation.directive';

describe('CalculationDirective', () => {
  it('should create an instance', () => {
    const directive = new CalculationDirective();
    expect(directive).toBeTruthy();
  });
});
