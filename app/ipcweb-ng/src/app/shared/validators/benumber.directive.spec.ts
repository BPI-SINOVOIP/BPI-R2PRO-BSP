import { BenumberDirective } from './benumber.directive';

describe('BenumberDirective', () => {
  it('should create an instance', () => {
    const directive = new BenumberDirective();
    expect(directive).toBeTruthy();
  });
});
